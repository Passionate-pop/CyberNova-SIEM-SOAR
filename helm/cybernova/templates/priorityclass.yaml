apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: {{ include "cybernova.fullname" . }}-critical
value: 1000
globalDefault: false
description: "Critical priority for CyberNova app and worker pods"
